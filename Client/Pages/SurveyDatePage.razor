@page "/dateSurvey"
@using Radzen




@*<RadzenDatePicker @bind-Value=@value DateFormat="d" Change=@OnChange Class="w-100" />*@
@code {
    @*DateTime? value = DateTime.Today;

    void OnChange(DateTime? value)
    {
        Console.WriteLine($"Value changed to {value}");
    }*@
}
